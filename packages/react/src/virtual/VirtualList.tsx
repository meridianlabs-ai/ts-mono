import { type VirtualListProps } from "./types";

export function VirtualList<T>(props: VirtualListProps<T>): never {
  void props;
  throw new Error("VirtualList placeholder — implementation lands in Task 1.M");
}
