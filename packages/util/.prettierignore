# Real-payload benchmark fixtures — huge JSON files, don't format
bench/fixtures/
# Benchmark result snapshots — machine-written JSON
bench/results/

# ESLint bulk-suppression ledger — machine-written JSON
eslint-suppressions.json
